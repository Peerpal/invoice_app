import 'package:flutter/material.dart';

class BoltComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Center(
        child: Text("Lightning Bolt"),
      ),
    );
  }
}